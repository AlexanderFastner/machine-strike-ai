{
  "name": "machine-strike",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @ms/web",
    "build": "npm run build -w @ms/web",
    "preview": "npm run preview -w @ms/web",
    "deploy": "npm run build && firebase deploy --only hosting",
    "typecheck": "tsc -p packages/ai && tsc -p packages/arena && tsc -p experiments && tsc -p packages/web",
    "test": "npm run typecheck && npm run test -w @ms/engine && npm run test -w @ms/arena",
    "test:fuzz:deep": "npm run test:fuzz:deep -w @ms/engine",
    "arena": "tsx packages/arena/src/cli.ts",
    "pieces:sheet": "tsx --tsconfig packages/web/tsconfig.json tools/piece-sheet.ts"
  },
  "devDependencies": {
    "@types/node": "^26.6.2"
  }
}
